<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:security="http://www.springframework.org/schema/security"

       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-4.2.xsd">

    <security:http auto-config="true" use-expressions="true">
        <security:intercept-url pattern="/bookTicket" access="hasRole('ROLE_BOOKING_MANAGER')"
                                requires-channel="http"></security:intercept-url>
        <security:intercept-url pattern="/**" access="hasAnyRole('ROLE_REGISTERED_USER', 'ROLE_BOOKING_MANAGER')"
                                requires-channel="http"></security:intercept-url>

        <!-- access denied page -->
        <security:access-denied-handler error-page="/403_AccessDenied"/>

        <security:form-login username-parameter="email"
                             password-parameter="password"></security:form-login>
        <security:logout logout-url="/logout"></security:logout>
    </security:http>

    <security:authentication-manager>
        <security:authentication-provider>
            <!--<security:user-service>
                <security:user name="o@g.com" password="1"
                               authorities="ROLE_REGISTERED_USER, ROLE_BOOKING_MANAGER"></security:user>
                <security:user name="o@e.com" password="1" authorities="ROLE_REGISTERED_USER"></security:user>
            </security:user-service>
-->            <security:jdbc-user-service data-source-ref="dataSource"
                                        users-by-username-query="select email, password, enabled from user where email=?"
                                        authorities-by-username-query="select email, roles from user where email=?"></security:jdbc-user-service>
        </security:authentication-provider>
    </security:authentication-manager>
</beans>